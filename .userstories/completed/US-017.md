# [US-017] - Placeholder extraction from DOCX

**Status:** ✅ DONE
**Start date:** 2026-04-02
**End date:** 2026-04-02
**Actual duration:** 1 day
**Priority:** 🔴 High
**Complexity:** M
**Epic:** Epic 3

**As a** system
**I want** to auto-detect all `{{ key }}` placeholders in a DOCX file
**So that** the manifest can be populated automatically

**Specification context:**
> F-306: Placeholder extraction handles split runs and headers/footers. Used for manifest population and validation warnings.

**Acceptance criteria:**
- [x] Extracts all `{{ key }}` placeholders from main body ✅
- [x] Extracts placeholders from headers and footers ✅
- [x] Handles split runs via run merger ✅
- [x] Returns deduplicated list of placeholder keys ✅
- [x] Collection markers (`{{ collection_key }}`, `{{ end }}`) excluded ✅
- [x] Conditional markers (`{{ if key }}`, `{{ end }}`) excluded ✅
- [x] Item placeholders (`{{ item.field }}`) included ✅

**Technical tasks:**
- [x] `TASK-017-01` - **[Class]** Create `DocxPlaceholderParser` in `Infrastructure/Rendering/` ✅ 2026-04-02
- [x] `TASK-017-02` - **[Method]** Implement `ExtractPlaceholders(WordprocessingDocument doc) → List<string>` ✅ 2026-04-02
- [x] `TASK-017-03` - **[Regex]** Match `{{ key }}` pattern (exclude `{{ if }}`, `{{ end }}`) ✅ 2026-04-02
- [x] `TASK-017-04` - **[Traversal]** Scan main body, headers, footers (after run merging) ✅ 2026-04-02
- [x] `TASK-017-05` - **[Deduplication]** Return unique set of keys ✅ 2026-04-02
- [x] `TASK-017-06` - **[Test]** Unit tests for placeholder extraction ✅ 2026-04-02
- [x] `TASK-017-07` - **[Test]** Unit tests for headers/footers extraction ✅ 2026-04-02

**Business rules:**
1. Placeholder extraction runs after run merging (prerequisite)
2. Collection/conditional markers are filtered out (not data placeholders)

**Dependencies:** US-013
**Estimation:** 3-4 days

## Implementation summary

- **`DocxPlaceholderParser`** (`Infrastructure/Rendering/DocxPlaceholderParser.cs`):
  - `ExtractPlaceholders(WordprocessingDocument)` → scans body + all HeaderParts + all FooterParts
  - `TokenPattern`: `\{\{\s*(?<key>[^{}\r\n]+?)\s*\}\}` — captures trimmed keys
  - Filters: `EndPattern` (`^end$`), `IfPattern` (`^if\s+`), `CollectionMarkerPattern` (standalone paragraph with single plain identifier)
  - `item.field` keys (containing dot) pass all filters and are included
  - Deduplication via `HashSet<string>` + `List<string>` preserving first-seen order

- **`DocxPlaceholderParserTests`** (`tests/.../Rendering/DocxPlaceholderParserTests.cs`):
  - 18 tests total: scalar extraction, deduplication, filtering, item.field, header/footer traversal, cross-section deduplication
  - All in-memory DOCX fixtures (no committed binary files)
