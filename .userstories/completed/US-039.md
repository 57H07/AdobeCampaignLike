# US-039: Fix CS8604 null-reference warnings in IAppLogger calls

- Status: DONE
- Completed: 2026-03-28
- Tasks: 5/5 implemented (0 partial)

## Files Created
(none)

## Files Modified
- src/CampaignEngine.Infrastructure/ApiKeys/ApiKeyService.cs
- src/CampaignEngine.Infrastructure/Campaigns/CampaignService.cs
- src/CampaignEngine.Infrastructure/Dispatch/LoggingDispatchOrchestrator.cs

## Implementation Summary
Applied null-coalescing fixes to all CS8604 warning sites using the `?? "unknown"` and `?.ToString() ?? "unknown"` patterns consistent with existing codebase conventions (matching SingleSendService.cs from US-037). In ApiKeyService.cs two nullable args were fixed: `createdByUserName ?? "unknown"` and `entity.ExpiresAt?.ToString() ?? "never"`. In CampaignService.cs: `createdBy ?? "unknown"` (line 148) and `campaign.ScheduledAt?.ToString() ?? "unknown"` (line 197). In LoggingDispatchOrchestrator.cs: `failedResult.ErrorDetail ?? "unknown"` (line 110). The solution now builds with zero CS8604 warnings and all 636 existing tests continue to pass.

## Partial Tasks
(none)

> Full US specification: see BACKLOG.md § US-039
